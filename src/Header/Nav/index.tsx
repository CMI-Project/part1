'use client'
import React from 'react'
import type { Header as HeaderType } from '@/payload-types'

// Legacy component — navigation is handled by HeaderClient (Component.client.tsx)
export const HeaderNav: React.FC<{ data: HeaderType }> = () => null
